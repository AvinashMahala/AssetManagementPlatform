import { OpenAPIV3 } from 'openapi-types';

export const usersSchemas: Record<string, OpenAPIV3.SchemaObject> = {
  // Users group doesn't have additional schemas beyond auth
  // All user-related schemas are in the auth group
};