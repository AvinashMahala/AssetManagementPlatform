import { OpenAPIV3 } from 'openapi-types';

// Unit utility schemas are already defined under units/schemas.ts (UnitUtility, UnitUtilityInput).
export const unitUtilitiesSchemas: Record<string, OpenAPIV3.SchemaObject> = {};
