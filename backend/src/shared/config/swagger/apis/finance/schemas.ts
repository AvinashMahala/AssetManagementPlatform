import { OpenAPIV3 } from 'openapi-types';

export const financeSchemas: Record<string, OpenAPIV3.SchemaObject> = {
  // Add finance-specific schemas here as needed (Expenses, Receipts, Transactions)
};
