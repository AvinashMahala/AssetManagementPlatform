# PropertyCard

A component that renders a card view for a single property, including details like name, address, type, status, and action buttons.

## Usage

```tsx
import { PropertyCard } from './PropertyCard';

<PropertyCard
  property={propertyData}
  isSelected={isSelected}
  onSelect={handleSelect}
  onDelete={handleDelete}
/>
```
