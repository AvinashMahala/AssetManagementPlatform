# MeterReadings

Meter reading endpoints for recording and reviewing utility readings.
<!-- 
- **Controller:** `MeterReadingsController`
- **Authentication:** Bearer token required
- **Endpoints included:**
  - `GET /api/meterreadings` — list
  - `GET /api/meterreadings/{id}` — get
  - `GET /api/meterreadings/meter/{meterId}` — list by meter
  - `POST /api/meterreadings` — create
  - `PUT /api/meterreadings/{id}` — update
  - `DELETE /api/meterreadings/{id}` — delete

Notes:
- Examples are simple JSON objects for create/update operations.

---

Use Swagger examples to create and verify readings. -->