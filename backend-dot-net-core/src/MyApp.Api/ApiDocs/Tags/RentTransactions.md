# RentTransactions

Ledger-style transactions for rent (payments, refunds, adjustments).
<!-- 
- **Controller:** `RentTransactionsController`
- **Authentication:** Public
- **Endpoints included:**
  - `GET /api/renttransactions` — list transactions
  - `GET /api/renttransactions/lease/{leaseId}` — by lease
  - `GET /api/renttransactions/property/{propertyId}` — by property
  - `GET /api/renttransactions/tenant/{tenantId}` — by tenant
  - `GET /api/renttransactions/unit/{unitId}` — by unit
  - `GET /api/renttransactions/{id}` — get transaction
  - `POST /api/renttransactions` — create transaction
  - `PUT /api/renttransactions/{id}` — update transaction
  - `DELETE /api/renttransactions/{id}` — delete transaction

Notes:
- Use small representative request examples; include `type` field (e.g., `payment`) for clarity.

---

Preview transactions with example payloads in Swagger UI. -->
