---
summary: List properties
description: |
  Returns a list of properties in the system. This endpoint returns an array of `PropertyDto` objects including id, name, address, ownerId and status.
tags: [Properties]
---

**Endpoint:** `GET /api/properties`

Use this endpoint to fetch all properties. No query parameters are required (current implementation returns all records).