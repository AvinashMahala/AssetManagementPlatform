---
summary: Create a tenant
tags:
  - Tenants
responses:
  201: "Tenant created"
---

**Endpoint:** `POST /api/tenants`

Create a new tenant record. Returns `201 Created` with created tenant.

**Authentication:** Bearer token required

Request example: `request.json`.
