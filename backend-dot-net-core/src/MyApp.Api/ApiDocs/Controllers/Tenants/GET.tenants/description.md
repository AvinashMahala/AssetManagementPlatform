---
summary: List tenants
tags:
  - Tenants
responses:
  200: "Array of tenants"
---

**Endpoint:** `GET /api/tenants`

Return all tenants. This endpoint is protected; a bearer token is required.

**Authentication:** Bearer token required

See `responses/200.json` for an example.
