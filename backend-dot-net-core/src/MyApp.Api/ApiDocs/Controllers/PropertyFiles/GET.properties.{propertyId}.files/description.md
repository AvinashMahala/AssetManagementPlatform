---
summary: List files for a property
description: |
  Returns metadata for files associated with the given property identified by `propertyId`.
  The response is an array of `FileMetadata` objects.
tags: [PropertyFiles]
---

**Endpoint:** `GET /api/properties/{propertyId}/files`