---
tags: [Files]
summary: List files with pagination and optional filtering by entity
---

**Endpoint:** `GET /api/files`

Returns a paginated list of files. Supports `page`, `limit`, `entityType`, and `entityId` query parameters.
