# BulkOperations

Docs for the BulkOperations controller endpoints (rent collection, payments, receipts, communication, export, validation).

Follow the ApiDocs/BLUEPRINT.md conventions: each endpoint is in its own `HTTPMETHOD.normalized-route` folder and contains `description.md`, optional `request.json`, `responses/*.json`, and `parameters.json` when applicable.